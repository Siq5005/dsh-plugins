/** Stable contract for the desktopProfiles Host service.
 * Provided by the desktop launcher; third-party plugins read it via
 * ctx.inject(['desktopProfiles']) or ctx.get('desktopProfiles').
 */

export const name = 'desktopProfiles'
