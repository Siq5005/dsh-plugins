# tools/ — 工具与 MCP

独立工具脚本与 MCP 服务。此类通常保持单文件或最小目录，便于单独取用。

```
tools/<tool-name>/
├── 可执行脚本或服务定义
└── README.md      # 用法说明
```

## 索引

新增工具后必须同步更新根目录 [`plugins.json`](../plugins.json) 与 [README 目录表](../README.md)。
